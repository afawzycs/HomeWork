// Explain what happens if you try to use a variable before it is declared in Dart.

void main() {
  // print(name); error because it is undeclared variable
  String name = "Ahmed";
  print(name);
}

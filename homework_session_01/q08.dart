// What is the difference between the num and double data types in Dart?

// num is a datatype which used when variable can hold both integer or decimal numbers

// double is a datatype which used when variable only hold floating-point numbers

void main() {
  num flexibleNumber = 39;
  print(flexibleNumber);
  flexibleNumber = 39.5;
  print(flexibleNumber);
}
